version: '3'
services:
  ldapserver:
      image: ghcr.io/lukasreschke/ldapserver-dev:602882d7ecdc86102b98043c826ce800aeff63c6
      volumes:
        - .:/workspace:cached
        - /var/run/docker.sock:/var/run/docker-host.sock
      command: /bin/sh -c "while sleep 1000; do :; done"

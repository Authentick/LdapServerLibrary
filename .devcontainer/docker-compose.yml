version: '3'
services:
  ldapserver:
      image: ghcr.io/lukasreschke/ldapserver-dev:218c2d150ddca416d9c1eed0885e882c8a6a6040
      volumes:
        - .:/workspace:cached
        - /var/run/docker.sock:/var/run/docker-host.sock
      command: /bin/sh -c "while sleep 1000; do :; done"

version: '3'
services:
  ldapserver:
      image: ghcr.io/lukasreschke/ldapserver-dev:sha-2b9cc27
      volumes:
        - .:/workspace:cached
        - /var/run/docker.sock:/var/run/docker-host.sock
      command: /bin/sh -c "while sleep 1000; do :; done"
